:source-highlighter: coderay

= Title

Some text

[source,makefile,linenums]
outputfile.txt:  # <1>
  echo "text" >$@    # <2>

<1> Declare the target to be 'outputfile.txt'
<2> Create the target

More text
